package main.java.shared.message;



/**enum that represents the entities that send the messages*/

public enum MessageSender {
    MODEL,
    CONTROLLER,
    SETUP_CONTROLLER,
    TURN_MANAGING_CONTROLLER,
    END_GAME_CONTROLLER
}
